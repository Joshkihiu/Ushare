{
  "name": "supermarket-terminal",
  "version": "1.0.0",
  "description": "Cashier web terminal: listens for a customer's number via ultrasonic chirp, cashier enters an amount, triggers an M-Pesa STK Push.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
